name: MiniBox scan server contract
on:
  push:
    branches: [minibox-network-mfp-runtime-v2]
  workflow_dispatch:
jobs:
  scan-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate scan service contract
        run: sh tests/test_scan_contract.sh
